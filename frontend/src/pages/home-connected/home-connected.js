import React, { useEffect, useState } from 'react';
import './home-connected.css';
import instance from '../../utils/config';
import Cookies from 'js-cookie';
import { useNavigate } from 'react-router-dom';

const HomeConnected = () => {
};

export default HomeConnected;
